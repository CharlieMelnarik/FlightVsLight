export type LatLon={lat:number;lon:number};
export function normalizeLonDiff(diff:number){while(diff>180)diff-=360;while(diff<-180)diff+=360;return diff;}
export function interpolateGreatCircle<T extends LatLon>(a:T,b:T,t:number):LatLon{const lat1=(a.lat*Math.PI)/180,lon1=(a.lon*Math.PI)/180,lat2=(b.lat*Math.PI)/180,lon2=(b.lon*Math.PI)/180;const A=[Math.cos(lat1)*Math.cos(lon1),Math.cos(lat1)*Math.sin(lon1),Math.sin(lat1)],B=[Math.cos(lat2)*Math.cos(lon2),Math.cos(lat2)*Math.sin(lon2),Math.sin(lat2)];const dot=Math.max(-1,Math.min(1,A[0]*B[0]+A[1]*B[1]+A[2]*B[2]));const omega=Math.acos(dot);if(omega===0)return a;const sinOmega=Math.sin(omega);const w1=Math.sin((1-t)*omega)/sinOmega,w2=Math.sin(t*omega)/sinOmega;const x=w1*A[0]+w2*B[0],y=w1*A[1]+w2*B[1],z=w1*A[2]+w2*B[2];return {lat:Math.atan2(z,Math.sqrt(x*x+y*y))*(180/Math.PI),lon:Math.atan2(y,x)*(180/Math.PI)}}
export function greatCircleDistanceNm(a:LatLon,b:LatLon){const R=3440.065;const lat1=(a.lat*Math.PI)/180,lon1=(a.lon*Math.PI)/180,lat2=(b.lat*Math.PI)/180,lon2=(b.lon*Math.PI)/180;const dLat=lat2-lat1,dLon=lon2-lon1;const sinLat=Math.sin(dLat/2),sinLon=Math.sin(dLon/2);const h=sinLat*sinLat+Math.cos(lat1)*Math.cos(lat2)*sinLon*sinLon;return R*(2*Math.atan2(Math.sqrt(h),Math.sqrt(1-h)));}
export function initialBearingDegrees(a:LatLon,b:LatLon){const lat1=(a.lat*Math.PI)/180,lat2=(b.lat*Math.PI)/180,dLon=((b.lon-a.lon)*Math.PI)/180;const y=Math.sin(dLon)*Math.cos(lat2),x=Math.cos(lat1)*Math.sin(lat2)-Math.sin(lat1)*Math.cos(lat2)*Math.cos(dLon);return (Math.atan2(y,x)*180)/Math.PI;}
export function splitWrappedPath(points:LatLon[],project:(lon:number,lat:number)=>{x:number;y:number},width:number){const segments:LatLon[][]=[];let current:LatLon[]=[];for(let i=0;i<points.length;i++){const p=points[i];if(i>0){const prevX=project(points[i-1].lon,points[i-1].lat).x;const x=project(p.lon,p.lat).x;if(Math.abs(x-prevX)>width*0.45){if(current.length)segments.push(current);current=[p];continue;}}current.push(p);}if(current.length)segments.push(current);return segments;}
